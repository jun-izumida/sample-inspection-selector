upstream app-service {
	server app:8000;
}

server {
	listen	80;

	location /sample-inspetion/app/static {}
		alias /staticfiles;
	}

	location ^~ /sample-inspection {
		alias /etc/nginx/html;
		try_files $uri $uri/ /index.html;
	}

	location /sample-inspection/app {
		proxy_pass http://sample_inspection_app:8000;
		proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
		proxy_set_header Host $http_host;
		proxy_redirect off;
		proxy_set_header X-Forwarded-Proto $scheme;
		proxy_set_header SCRIPT_NAME /sample-inspection/app;
	}

	error_page	500 502 503 504	/50x.html;
	location = /50x.html {
		root	/usr/share/nginx/html;
	}
}